# LUNA Learning Platform

LUNA is a web-based learning environment built with Node.js, SQLite and a simple HTML/CSS/JS front end. It is a prototype that demonstrates how the core components of a course catalog might work.

## Getting Started

Install dependencies:

```bash
npm install
```

Run the development server:

```bash
npm start
```

Then open `http://localhost:3000` in your browser.
